{
  "name": "rwa-infra-token",
  "version": "1.0.0",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run --network sepolia deploy/01-deploy.js"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.0"
  },
  "devDependencies": {
    "dotenv": "^16.0.0",
    "hardhat": "^2.17.0",
    "@nomiclabs/hardhat-ethers": "^2.1.0",
    "ethers": "^6.0.0"
  }
}
